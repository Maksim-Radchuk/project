import React from 'react';
import DeviceList from '../components/DeviceList.js';

const Distillers = () => {
    return (
        <div className="distillers">
            <DeviceList/>
        </div>
    );
};

export default Distillers;